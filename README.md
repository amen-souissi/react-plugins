```bash
cd react-plugins-core
npm install && npm pack
cd ../plugin-foo
npm install && npm pack
cd ../plugin-bar
npm install && npm pack
cd ../examples/my-app
npm install && npm start
```

Or use npm link
